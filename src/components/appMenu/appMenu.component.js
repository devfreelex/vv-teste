import template from './appMenu.template'
import styles from './appMenu.styles'

export default () => {

    const tagName = 'app-menu'

    return {
        tagName,
        template,
        styles
    }
}