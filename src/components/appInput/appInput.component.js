import template from './appInput.template'
import styles from './appInput.styles'

export default () => {
    const tagName = 'app-input'

    return {
        tagName,
        template,
        styles
    }
}