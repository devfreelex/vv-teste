import template from './appButton.template'
import styles from './appButton.styles'

export default () => {

    const tagName = 'app-button'

    return {
        tagName,
        template,
        styles
    }
}