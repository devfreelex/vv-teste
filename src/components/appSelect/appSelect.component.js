import template from './appSelect.template'
import styles from './appSelect.styles'

export default () => {
    const tagName = 'app-select'

    return {
        tagName,
        template,
        styles
    }
}