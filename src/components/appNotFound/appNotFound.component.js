import template from './appNotFound.template'
import styles from './appNotFound.styles'

export default () => {
    const tagName = 'app-not-found'

    return {
        tagName,
        template,
        styles
    }
}